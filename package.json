{
  "name": "logscann",
  "version": "1.0.0",
  "description": "LogScann — Saída Flex Velozz PWA",
  "private": true,
  "scripts": {
    "format": "prettier --write \"**/*.{js,css,html,json,md}\"",
    "format:check": "prettier --check \"**/*.{js,css,html,json,md}\""
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
